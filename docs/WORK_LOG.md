# Work Log

Append-only project history. New entries go at the bottom.

## Entries

- 2026-06-12 12:24 IST - Codex - Phase 0 - Began Phase 0 scaffold after reading `PLAN.md`; initialized git and started creating project-memory files - Files changed: `AGENTS.md`, `PROJECT_CONTEXT.md`, `docs/PROJECT_MEMORY.md`, `docs/WORK_LOG.md`, `docs/PHASE_STATUS.md` - Tests run: pending - Result: in progress - Next step: complete scaffold and run validation.
- 2026-06-12 12:33 IST - Codex - Phase 0 - Completed Phase 0 scaffold, docs, environment defaults, Spring Boot gateway skeleton, CI, compose placeholders, and router labels - Files changed: Phase 0 scaffold files - Tests run: router-label validation, `mvn verify`, packaged gateway boot check - Result: passed - Next step: stop for human checkpoint.
- 2026-06-12 12:43 IST - Codex - Phase 1 - Documented Moshi protocol from Kyutai source; added `/ws/voice`, session lifecycle, Moshi clients, fake Moshi stub, static browser shell, and WebSocket integration tests - Files changed: `docs/moshi-protocol.md`, gateway `ws/session/speech` classes, static web files, fake Moshi files, README, memory files - Tests run: router-label validation, `mvn verify` - Result: passed - Next step: stop for Phase 1 human checkpoint.
- 2026-06-12 12:53 IST - Codex - Phase 2 - Added STT contracts and sidecar scaffold, transcript buffer/service, heuristic and real-router scaffolds, LLM client boundaries, route-decision browser events, JSONL event logging, ACT canned response, offline router eval, browser utterance input, and Phase 2 tests - Files changed: gateway router/transcript/speech/session/ws/static classes, `stt-service`, `scripts/router_eval.py`, README, compose, CI, memory files - Tests run: router-label validation, router eval, `mvn verify` - Result: passed - Next step: stop for Phase 2 human checkpoint and run real-LLM router eval.
- 2026-06-12 13:03 IST - Codex - Real Moshi probe - Found local `moshi_mlx 0.3.0` venv and cached `kyutai/moshiko-mlx-q4`; started `moshi_mlx.local_web` on port 8998, confirmed `/api/chat` handshake `00`, then ran gateway with `MOSHI_MODE=real` on port 18080 - Files changed: memory docs only - Tests run: direct Moshi WebSocket handshake probe, gateway real-Moshi WebSocket probe - Result: real Moshi is installed and connectable, but current V2 cannot replace stub for audio because raw PCM is sent where Moshi expects Ogg/Opus; Moshi closed with `unexpected ogg capture pattern [0, 1, 2, 3]` - Next step: add PCM<->Opus bridge or a Moshi-native relay mode before using real Moshi for `/ws/voice`.
- 2026-06-12 13:17 IST - Codex - Real Moshi bridge - Added pure-Java Ogg/Opus encode/decode bridge for `RealMoshiClient`, wired real mode to preserve raw PCM browser contract while speaking Moshi Ogg/Opus, added AudioWorklet browser mic capture, and fixed close-race control-message handling - Files changed: gateway `ws` codec/client classes, static browser files, `gateway/pom.xml`, README, memory files - Tests run: `mvn -pl gateway -Dtest=OggOpusCodecTests test`, router-label validation, router eval, `mvn verify`, real-Moshi `/ws/voice` smoke test with `moshi_mlx.local_web` - Result: passed; gateway real mode returned decoded PCM from real Moshi without Ogg errors - Next step: run human mic/speaker conversation through `http://localhost:8080` with `MOSHI_MODE=real`, then continue Phase 2 real-LLM checkpoint.
- 2026-06-12 13:30 IST - Codex - Real Moshi playback fix - Investigated inaudible browser response; real spoken-phrase probe returned Moshi text and 256 PCM chunks but raw peak was only about 0.039, so the browser now unlocks audio on user gestures, queues streamed PCM sequentially, logs raw/output peaks, provides a speaker test, and applies 6x clipped output gain - Files changed: static browser files, README, memory files - Tests run: JS syntax check, router-label validation, router eval, `mvn -pl gateway -Dtest=OggOpusCodecTests test`, `mvn verify`, real-Moshi spoken-phrase `/ws/voice` probe - Result: backend path is live and browser output should be audible at higher gain - Next step: user retests in browser with `Test Speaker`, `Connect`, and `Start Mic`.
- 2026-06-12 13:41 IST - Codex - Real Moshi decoder-rate fix - Root-caused gibberish speaker output to `OggOpusDecoder` forcing Moshi `sphn` response packets through a 24 kHz Opus decoder even though Moshi's OpusHead declares a 48 kHz input rate; changed response decode to 48 kHz with downsampling to browser 24 kHz PCM, reduced browser gain to unity, and added a Moshi-style `sphn` fixture test - Files changed: `OggOpusDecoder`, codec tests, static browser JS, README, memory files - Tests run: JS syntax check, router-label validation, router eval, `mvn verify`, Moshi `sphn` fixture decode test, clean real-Moshi spoken-phrase `/ws/voice` probe - Result: passed; real probe returned 26 text/control messages, 216 binary PCM chunks, and max decoded peak about 0.5909 - Next step: restart Moshi and gateway normally, hard refresh browser, retest voice.
- 2026-06-12 15:38 IST - Codex - Phase 3 - Implemented ASK flow end-to-end in stub mode: answer jobs with correlation IDs, supersede/stale policy, answer and harmonizer prompts, TTS client boundary, TTS sidecar scaffold, outbound mixer injection, Moshi suppression during injection, expanded state machine, injection control messages, and Phase 3 acceptance tests - Files changed: gateway jobs/llm/session/speech/ws classes, prompts, `tts-service`, compose, README, memory files - Tests run: `mvn -pl gateway -Dtest=SessionStateMachineTests,Phase3AskFlowIntegrationTests test`, `mvn -pl gateway verify`, router-label validation, Python compile checks, JS syntax checks, `git diff --check` - Result: passed - Next step: stop for Phase 3 human checkpoint with real Moshi plus backend answer path.
- 2026-06-12 19:54 IST - Codex - Phase 3 TTS checkpoint fix - Replaced the local TTS sidecar's placeholder-only behavior with a macOS `say` + `afconvert` spoken WAV path while keeping deterministic tone fallback for non-macOS/CI; clarified that Java `TTS_MODE=stub` intentionally beeps - Files changed: `tts-service/app/main.py`, `tts-service/README.md`, README, memory files - Tests run: macOS `say`/`afconvert` WAV conversion probe, Python compile check - Result: sidecar can produce 24 kHz mono spoken WAV on this Mac - Next step: run `tts-service` and start the gateway with `TTS_MODE=real` to hear speech instead of beep.
- 2026-06-12 20:05 IST - Codex - TTS sidecar setup fix - Diagnosed failed `pydantic-core` install as Python 3.14/PyO3 compatibility with pinned dependencies; rebuilt `tts-service/.venv` using `/opt/homebrew/bin/python3.12`, verified `/health` returns `engine=macos_say`, verified `/speak` returns 24 kHz mono WAV, and updated setup docs plus `.gitignore` for nested virtualenvs - Files changed: `.gitignore`, README files, memory files - Tests run: TTS venv install, TTS `/health`, TTS `/speak`, WAV format check - Result: passed - Next step: start TTS sidecar from the Python 3.12 venv and run gateway with `TTS_MODE=real`.
- 2026-06-12 20:11 IST - Codex - Real TTS injection fix - Investigated browser logs where `inject.start` appeared but no audio or `inject.end`; changed `RealTtsClient` to defer blocking TTS fetch into the reactive stream, changed `OutboundMixer` to pace PCM frames at 80 ms/frame and surface TTS stream errors, removed stub TTS self-delay, and verified a real-TTS WebSocket smoke path - Files changed: gateway speech classes, memory files - Tests run: `mvn -pl gateway -Dtest=SessionStateMachineTests,Phase3AskFlowIntegrationTests test`, `mvn -pl gateway verify`, real-TTS smoke on port 18080 with TTS sidecar on 8082 - Result: passed; smoke received `inject.start`, 24 binary PCM frames, and `inject.end` - Next step: restart gateway on port 8080 with patched jar and `TTS_MODE=real`.
- 2026-06-12 20:16 IST - Codex - Real TTS buffer fix - Diagnosed `TTS_STREAM_FAILED` with `DataBufferLimitException: Exceeded limit on max bytes to buffer : 262144`; changed `RealTtsClient` to consume `/speak` as streamed `DataBuffer` chunks instead of `byte[]`, preserving WAV header stripping and PCM splitting - Files changed: `RealTtsClient`, memory files - Tests run: `mvn -pl gateway -Dtest=SessionStateMachineTests,Phase3AskFlowIntegrationTests test`, `mvn -pl gateway verify`, real-TTS smoke on port 18080 with TTS sidecar on 8082 - Result: passed; smoke received `inject.start`, 24 binary PCM frames, no error, and `inject.end` - Next step: start TTS sidecar and restart gateway with patched jar plus `TTS_MODE=real`.
- 2026-06-12 20:24 IST - Codex - Real Moshi test session - Started Moshi and the gateway in detached `screen` sessions so they persist for browser testing; verified TTS sidecar on 8082, Moshi on 8998, and gateway on 8080 - Files changed: memory docs only - Tests run: WebSocket handshake through `/ws/voice`; synthesized speech clip sent through gateway to real Moshi - Result: passed; received `session.start`, Moshi text fragments `Hey`/`what`, and 6 binary PCM response frames - Next step: user runs the browser mic/speaker Phase 3 checkpoint at `http://localhost:8080`.
- 2026-06-12 20:29 IST - Codex - Backend handoff smoke - Tested the gateway handoff path with real Moshi connected, real backend LLM enabled from local `.env`, and real TTS on 8082 - Files changed: memory docs only - Tests run: WebSocket typed `transcript.user` for `what is the capital of australia` - Result: passed; observed `router.decision` `ASK` confidence `0.95`, backend answer, `inject.start`, 59 binary PCM frames, and `inject.end` - Next step: user tests the same flow in the browser by typing a factual question with `Send Utterance` while real Moshi is connected.
- 2026-06-12 20:40 IST - Codex - Phase 4 - Implemented ASK-in-flight suppression and barge-in cancellation: configurable token threshold/fade, Moshi long-answer audio fading, acknowledgment pass-through, cancellable TTS injections, user-speech barge-in detection, and fake-Moshi fixtures - Files changed: gateway suppression/barge-in/mixer/session/ws/config classes, Phase 4 tests, fake Moshi, `.env.example`, README, memory files - Tests run: focused Phase 4 tests, `mvn -pl gateway verify`, router-label validation, router eval, Python compile checks, JS syntax checks, `git diff --check` - Result: passed - Next step: stop for Phase 4 human checkpoint and record real suppression rate.
- 2026-06-12 20:57 IST - Codex - Phase 5 - Implemented metrics and evaluation: Micrometer event mirroring, Prometheus endpoint exposure, `moshi.first_audio` tracking, Prometheus/Grafana provisioning, fixture event logs, offline latency/suppression/router analysis, and stub/real flow-break judge - Files changed: gateway metrics/session/ws config, `metrics/`, `ops/`, compose, CI, README, memory files - Tests run: `mvn -pl gateway verify`, metrics fixture analysis, stub flow judge, router-label validation, router eval, Python compile checks, JS syntax checks, Grafana dashboard JSON validation, `git diff --check` - Result: passed - Next step: stop for Phase 5 human checkpoint and generate real runtime metrics chart/table.
- 2026-06-13 14:31 IST - Codex - Phase 6 - Implemented hardening and packaging: opt-in `/ws/voice` bearer-token auth, browser auto-reconnect, real sidecar startup health retries, LLM retry/fallback, Moshi connection-error reset, pinned/health-checked Dockerfiles, CPU/GPU compose packaging, CI image builds, final README sections, and concurrent-session isolation tests - Files changed: gateway config/ws/jobs/llm/static/tests, Dockerfiles, compose files, CI, README, architecture, memory files - Tests run: focused Phase 6 tests, gateway verify, router/metrics scripts, Python compile checks, JS syntax checks, compose config validation, Docker image build, `git diff --check` - Result: passed - Next step: stop for Phase 6 human checkpoint and record full real-runtime demo video.
- 2026-06-13 15:07 IST - Codex - Flow diagnostic helper - Added `scripts/flow_log.py` to summarize `data/events.jsonl` into `data/flow-log.md` so Moshi-only turns and backend handoffs can be distinguished during the real-runtime checkpoint - Files changed: `scripts/flow_log.py`, README, CI, memory files - Tests run: `python3 -m py_compile scripts/flow_log.py`, `python3 scripts/flow_log.py data/events.jsonl --out data/flow-log.md --tail 12`, `python3 scripts/flow_log.py metrics/fixtures/events.jsonl --out metrics/out/flow-log.md` - Result: passed; current runtime log shows recent successful `BACKEND_SPOKEN` handoff turns - Next step: run a fresh real-Moshi browser test, then regenerate `data/flow-log.md`.
- 2026-06-13 15:43 IST - Codex - Real STT activation - Implemented real microphone STT path so spoken mic audio can route to the backend: Java buffers voiced PCM and posts WAV utterances to `stt-service`, while the sidecar uses lazy `faster-whisper` transcription - Files changed: gateway STT config/client/tests, `stt-service`, compose files, README, memory files - Tests run: `mvn -pl gateway -Dtest=RealSttClientTests test`, `mvn -pl gateway verify`, Python compile checks, compose config validation, `docker compose build stt`, `docker compose build gateway`, STT `/health`, real `/transcribe` with generated speech WAV - Result: passed; STT returned `What is the capital of Australia?` - Next step: run full real Moshi/STT/LLM/TTS browser demo and inspect `data/flow-log.md`.
- 2026-06-13 15:53 IST - Codex - Docker TTS speech fix - Checked logs for beep-only backend audio; STT was producing transcriptions and TTS `/speak` was called, but TTS health reported `stub_sine` in Docker - Files changed: `tts-service/app/main.py`, `tts-service/Dockerfile`, `tts-service/README.md`, `.env.example`, `docker-compose.yml`, README, memory files - Tests run: Python compile check, compose config check, `docker compose -f docker-compose.yml -f docker-compose.gpu.yml build tts`, recreated TTS, TTS `/health`, TTS `/speak` WAV probe - Result: passed; TTS health now reports `engine=espeak_ng` and `/speak` returns a 24 kHz mono WAV - Next step: retest browser backend injection; it should be spoken espeak audio instead of beeps.
- 2026-06-13 16:04 IST - Codex - Rich handoff logging - Added conversation-level handoff diagnostics for debugging unnatural Moshi/backend overlap: gateway JSONL now records user query text, Moshi text, router input/decision, backend answer LLM request/response, harmonizer input/output, final injected TTS text, and timing; `scripts/flow_log.py` renders those fields into `data/flow-log.md` - Files changed: gateway coordinator/job/LLM logging classes, `scripts/flow_log.py`, README, memory files - Tests run: `python3 -m py_compile scripts/flow_log.py`, fixture flow-log generation, `mvn -pl gateway -Dtest=Phase3AskFlowIntegrationTests,BrowserSocketHandlerIntegrationTests test`, `git diff --check`, Docker gateway image build, Docker gateway/STT/TTS health checks - Result: passed; gateway/STT/TTS are healthy, but Moshi was not listening on port 8998 during this update - Next step: start Moshi, run a fresh factual-question browser handoff, then regenerate `data/flow-log.md`.
- 2026-06-14 13:42 IST - Codex - Runtime issue tracker - Created root `issues.md` from the latest real-runtime `data/events.jsonl` and `data/flow-log.md`, capturing superseded ASK injection, overlapping/out-of-order TTS injection, Moshi factual speech during handoff, stale STT routing, transcript pollution, compose startup fragility, and remaining checkpoint gaps - Files changed: `issues.md`, memory files - Tests run: documentation-only change, no automated tests - Result: issue tracker created - Next step: fix issue 1, blocking superseded ASK jobs before harmonizer/TTS injection.
- 2026-06-14 13:55 IST - Codex - Qwen3-4B plan pivot - Added a top-level `PLAN.md` `CHANGES` section that replaces the Moshi-first runtime direction with a Qwen3-4B fast conversational layer over STT/TTS, including revised states, `CHAT` and `ASK_PENDING` flows, required components, prompt contracts, event names, metrics, config keys, migration plan, and acceptance criteria - Files changed: `PLAN.md`, `docs/decisions.md`, memory files - Tests run: documentation-only change, pending diff check - Result: plan updated - Next step: implement `FastConversationService` and the new Qwen-first state flow.
- 2026-06-14 14:04 IST - Codex - Local Qwen runtime setup - Installed Ollama 0.30.8 through Homebrew, started the Ollama service, pulled local `qwen3:4b`, and verified native plus OpenAI-compatible API probes on `localhost:11434` - Files changed: memory files only - Tests run: `ollama list`, `curl http://localhost:11434/api/version`, `/api/generate` probe, `/v1/chat/completions` probe - Result: model is local and reachable; local `qwen3:4b` still emits thinking/reasoning output, so the upcoming integration needs a thinking-output guard before TTS - Next step: implement `FastConversationService` against local Ollama with safe spoken-text filtering.
- 2026-06-14 14:20 IST - Codex - Qwen runtime implementation - Added opt-in `VOICE_RUNTIME=qwen` gateway flow with `FastConversationService`, Qwen `CHAT` and `ASK_PENDING` prompts, fast/backend transcript labels, pending-answer registry, backend injection queueing, one-active-TTS enforcement, Qwen event logging, flow-log support, compose/env wiring, README run docs, and Qwen WebSocket integration tests - Files changed: gateway config/llm/session/speech/transcript/ws classes, prompt resources, tests, compose, README, memory files - Tests run: focused Qwen/state tests, `mvn -pl gateway verify`, router-label validation, router eval, flow-log Python compile, browser JS syntax, compose config checks, `git diff --check` - Result: passed - Next step: run a real local Qwen/STT/TTS browser test and inspect `data/flow-log.md`.
- 2026-06-14 14:50 IST - Codex - Qwen2.5 output diagnosis - Checked latest `data/events.jsonl` and regenerated `data/flow-log.md` after switching fast model to `qwen2.5:1.5b` - Files changed: `data/flow-log.md`, memory files - Tests run: `python3 scripts/flow_log.py data/events.jsonl --out data/flow-log.md --tail 12` - Result: `qwen2.5:1.5b` responded faster and without reasoning output, but incorrectly answered the pending factual question during `ASK_PENDING` with `Well, that's Canberra!` - Next step: implement code-level `ASK_PENDING` safe-intent or factual-answer guard before TTS.
- 2026-06-16 14:38 IST - Codex - Fast-layer safety note - Added a small three-step design doc for the interactive fast layer: fine-tune for style, sanitize `ASK_PENDING` prompts, and verify drafts before TTS - Files changed: `docs/fast-layer-safety.md`, memory files - Tests run: documentation-only change, `git diff --check` - Result: safety approach documented - Next step: implement sanitized prompt construction plus verifier-gated TTS.
- 2026-06-16 14:43 IST - Codex - Fast-layer fine-tuning workspace - Created `fast-layer-finetune/` as the local LoRA workspace for `Qwen/Qwen2.5-1.5B-Instruct`, including purpose docs, decisions/states, data guide, and gitignored output directories - Files changed: `fast-layer-finetune/`, memory files - Tests run: documentation/layout-only change, `git diff --check` - Result: fine-tuning workspace created - Next step: create `train.jsonl` and `valid.jsonl` examples for the fast-layer states.
- 2026-06-16 15:08 IST - Codex - Fast-layer dataset cleanup - Rewrote the canonical `fast-layer-finetune/data/train.jsonl` and `valid.jsonl` assistant targets for better spoken range, removed exact duplicate replies, preserved state distribution, kept pending-ASK targets non-factual, and updated the fine-tuning data docs for the actual chat JSONL schema - Files changed: `fast-layer-finetune/data/train.jsonl`, `fast-layer-finetune/data/valid.jsonl`, `fast-layer-finetune/README.md`, `fast-layer-finetune/data/README.md`, memory files - Tests run: JSONL structure/stats validation for train and validation data - Result: passed; train has 300 unique assistant targets, validation has 50 unique assistant targets, all replies are under 20 words, and no obvious hard factual leaks were found - Next step: start the first LoRA training run.
- 2026-06-16 16:15 IST - Codex - ASK_PENDING sanitizer - Added `AskPendingPromptSanitizer` and wired `FastConversationService` so Qwen `ASK_PENDING` prompts receive only abstract state/context/intent/rules instead of the raw factual question, latest user text, or transcript window - Files changed: gateway fast LLM classes/tests, memory files - Tests run: `mvn -pl gateway -Dtest=AskPendingPromptSanitizerTests,QwenRuntimeIntegrationTests test` - Result: passed; sanitizer tests cover normal and slang geography prompts plus a medical prompt, and Qwen integration logs no longer expose the raw Australia question in `fast.reply.request` - Next step: add verifier-gated TTS for `ASK_PENDING` fast replies.
- 2026-06-16 21:13 IST - Codex - Router slang ASK fix - Followed up on manual sanitizer test where `whats the cap of aus` was routed as `CHAT`; added heuristic ASK cues for `whats`, `cap of ...`, and `capital of ...` - Files changed: `HeuristicRouter`, router tests, memory files - Tests run: `mvn -pl gateway -Dtest=HeuristicRouterTests,AskPendingPromptSanitizerTests,QwenRuntimeIntegrationTests test` - Result: passed; slang capital queries now route to `ASK` so sanitizer can run - Next step: retest the manual WebSocket sanitizer flow, then add verifier-gated TTS.
- 2026-06-16 21:30 IST - Codex - Timing-only diagnostics - Added millisecond timing events for STT, router, sanitizer, fast Qwen, backend LLM, harmonizer, and TTS first/total durations, plus `scripts/timing_log.py` to write `data/timing-log.md` without conversation text - Files changed: gateway coordinator/STT/fast-LLM classes, `scripts/timing_log.py`, README, memory files - Tests run: `mvn -pl gateway -Dtest=RealSttClientTests,QwenRuntimeIntegrationTests,HeuristicRouterTests test`, `mvn -pl gateway verify`, `python3 -m py_compile scripts/timing_log.py scripts/flow_log.py`, timing-log generation from `data/events.jsonl` and `metrics/fixtures/events.jsonl`, `git diff --check` - Result: passed; older rows show `-` until fresh instrumented gateway turns are recorded - Next step: add verifier-gated TTS for `ASK_PENDING` fast replies.
- 2026-06-16 21:35 IST - Codex - Timing-log readability - Simplified `data/timing-log.md` formatting so values under one second show as `ms`, values over one second show as seconds, TTS columns are collapsed to front/backend totals, and a `slowest` column identifies the bottleneck - Files changed: `scripts/timing_log.py`, `data/timing-log.md`, `metrics/out/timing-log.md`, README, memory files - Tests run: `python3 -m py_compile scripts/timing_log.py`, timing-log generation from `data/events.jsonl` and `metrics/fixtures/events.jsonl` - Result: passed; `2203 ms` now displays as `2.20s` - Next step: add verifier-gated TTS for `ASK_PENDING` fast replies.
- 2026-06-16 21:38 IST - Codex - Timing-log labeled cells - Updated the timing report so every timing cell repeats the step name with the duration, such as `STT: -` or `front LLM: 2.20s` - Files changed: `scripts/timing_log.py`, `data/timing-log.md`, `metrics/out/timing-log.md`, README, memory files - Tests run: `python3 -m py_compile scripts/timing_log.py`, timing-log generation from `data/events.jsonl` and `metrics/fixtures/events.jsonl` - Result: passed - Next step: add verifier-gated TTS for `ASK_PENDING` fast replies.
- 2026-06-16 22:13 IST - Codex - Real local runtime start - Started the real Qwen/STT/TTS/backend stack in detached `screen` sessions and smoke-tested typed WebSocket turns - Files changed: `data/events.jsonl`, `data/timing-log.md`, `data/flow-log.md`, memory files - Tests run: gateway/STT/TTS/Ollama health checks, typed `CHAT` WebSocket smoke, typed `ASK` backend handoff smoke, timing/flow log regeneration - Result: passed; services are running as `stt8081`, `tts8082`, and `gateway8080`; latest typed ASK row shows router `1.33s`, sanitizer `3ms`, front LLM `461ms`, backend LLM `1.01s`, harmonizer `923ms`, backend TTS `8.06s` - Next step: user tests live browser mic at `http://127.0.0.1:8080`, then inspect `data/timing-log.md` for STT timings.
- 2026-06-16 22:25 IST - Codex - Git publish prep - Added simple `RUN.md` startup instructions and `EXPLANATION.md` project overview for future LLM/developer handoff; updated `.gitignore` so `.env.*`, runtime `data/`, `gateway/data/`, and `fast-layer-finetune/` stay local-only - Files changed: `RUN.md`, `EXPLANATION.md`, `.gitignore`, memory files - Tests run: `mvn -pl gateway verify`, router-label validation, router eval, Python compile checks, JS syntax check, `git diff --check` - Result: passed; no git remote is configured yet - Next step: add a remote and push `main`, then continue with verifier-gated `ASK_PENDING` TTS.
- 2026-06-16 22:33 IST - Codex - GitHub push - Added `origin` remote at `https://github.com/Ritesh102000/DuplexResponseV1.git`, pushed `main`, and updated memory docs to reflect the remote/tracking state - Files changed: memory files - Tests run: `git push -u origin main` - Result: passed; `main` tracks `origin/main` - Next step: continue with verifier-gated `ASK_PENDING` TTS.
- 2026-06-17 19:04 IST - Codex - Real mode runtime check - Verified the existing detached real-mode stack instead of restarting it - Files changed: local `data/events.jsonl` plus memory files - Tests run: gateway/STT/TTS/Ollama health checks and typed WebSocket `CHAT` smoke - Result: passed; gateway `8080`, STT `8081`, TTS `8082`, and Ollama `11434` are healthy, Qwen returned `Hello!`, and TTS streamed audio frames - Next step: open `http://127.0.0.1:8080` and test with the browser mic.
- 2026-06-17 19:09 IST - Codex - Stop real mode runtime - Stopped the project gateway/STT/TTS services after the user requested stop - Files changed: memory files - Tests run: `screen -ls`, port-listener check for `8080`, `8081`, and `8082` - Result: passed; no project screen sessions remain and ports `8080`, `8081`, and `8082` are closed; Ollama remains running on `11434` - Next step: use `RUN.md` to start the stack again when needed.
