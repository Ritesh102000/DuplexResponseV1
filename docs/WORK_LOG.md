# Work Log

Append-only project history. New entries go at the bottom.

## Entries

- 2026-06-12 12:24 IST - Codex - Phase 0 - Began Phase 0 scaffold after reading `PLAN.md`; initialized git and started creating project-memory files - Files changed: `AGENTS.md`, `PROJECT_CONTEXT.md`, `docs/PROJECT_MEMORY.md`, `docs/WORK_LOG.md`, `docs/PHASE_STATUS.md` - Tests run: pending - Result: in progress - Next step: complete scaffold and run validation.
- 2026-06-12 12:33 IST - Codex - Phase 0 - Completed Phase 0 scaffold, docs, environment defaults, Spring Boot gateway skeleton, CI, compose placeholders, and router labels - Files changed: Phase 0 scaffold files - Tests run: router-label validation, `mvn verify`, packaged gateway boot check - Result: passed - Next step: stop for human checkpoint.
- 2026-06-12 12:43 IST - Codex - Phase 1 - Documented Moshi protocol from Kyutai source; added `/ws/voice`, session lifecycle, Moshi clients, fake Moshi stub, static browser shell, and WebSocket integration tests - Files changed: `docs/moshi-protocol.md`, gateway `ws/session/speech` classes, static web files, fake Moshi files, README, memory files - Tests run: router-label validation, `mvn verify` - Result: passed - Next step: stop for Phase 1 human checkpoint.
- 2026-06-12 12:53 IST - Codex - Phase 2 - Added STT contracts and sidecar scaffold, transcript buffer/service, heuristic and real-router scaffolds, LLM client boundaries, route-decision browser events, JSONL event logging, ACT canned response, offline router eval, browser utterance input, and Phase 2 tests - Files changed: gateway router/transcript/speech/session/ws/static classes, `stt-service`, `scripts/router_eval.py`, README, compose, CI, memory files - Tests run: router-label validation, router eval, `mvn verify` - Result: passed - Next step: stop for Phase 2 human checkpoint and run real-LLM router eval.
- 2026-06-12 13:03 IST - Codex - Real Moshi probe - Found local `moshi_mlx 0.3.0` venv and cached `kyutai/moshiko-mlx-q4`; started `moshi_mlx.local_web` on port 8998, confirmed `/api/chat` handshake `00`, then ran gateway with `MOSHI_MODE=real` on port 18080 - Files changed: memory docs only - Tests run: direct Moshi WebSocket handshake probe, gateway real-Moshi WebSocket probe - Result: real Moshi is installed and connectable, but current V2 cannot replace stub for audio because raw PCM is sent where Moshi expects Ogg/Opus; Moshi closed with `unexpected ogg capture pattern [0, 1, 2, 3]` - Next step: add PCM<->Opus bridge or a Moshi-native relay mode before using real Moshi for `/ws/voice`.
- 2026-06-12 13:17 IST - Codex - Real Moshi bridge - Added pure-Java Ogg/Opus encode/decode bridge for `RealMoshiClient`, wired real mode to preserve raw PCM browser contract while speaking Moshi Ogg/Opus, added AudioWorklet browser mic capture, and fixed close-race control-message handling - Files changed: gateway `ws` codec/client classes, static browser files, `gateway/pom.xml`, README, memory files - Tests run: `mvn -pl gateway -Dtest=OggOpusCodecTests test`, router-label validation, router eval, `mvn verify`, real-Moshi `/ws/voice` smoke test with `moshi_mlx.local_web` - Result: passed; gateway real mode returned decoded PCM from real Moshi without Ogg errors - Next step: run human mic/speaker conversation through `http://localhost:8080` with `MOSHI_MODE=real`, then continue Phase 2 real-LLM checkpoint.
- 2026-06-12 13:30 IST - Codex - Real Moshi playback fix - Investigated inaudible browser response; real spoken-phrase probe returned Moshi text and 256 PCM chunks but raw peak was only about 0.039, so the browser now unlocks audio on user gestures, queues streamed PCM sequentially, logs raw/output peaks, provides a speaker test, and applies 6x clipped output gain - Files changed: static browser files, README, memory files - Tests run: JS syntax check, router-label validation, router eval, `mvn -pl gateway -Dtest=OggOpusCodecTests test`, `mvn verify`, real-Moshi spoken-phrase `/ws/voice` probe - Result: backend path is live and browser output should be audible at higher gain - Next step: user retests in browser with `Test Speaker`, `Connect`, and `Start Mic`.
- 2026-06-12 13:41 IST - Codex - Real Moshi decoder-rate fix - Root-caused gibberish speaker output to `OggOpusDecoder` forcing Moshi `sphn` response packets through a 24 kHz Opus decoder even though Moshi's OpusHead declares a 48 kHz input rate; changed response decode to 48 kHz with downsampling to browser 24 kHz PCM, reduced browser gain to unity, and added a Moshi-style `sphn` fixture test - Files changed: `OggOpusDecoder`, codec tests, static browser JS, README, memory files - Tests run: JS syntax check, router-label validation, router eval, `mvn verify`, Moshi `sphn` fixture decode test, clean real-Moshi spoken-phrase `/ws/voice` probe - Result: passed; real probe returned 26 text/control messages, 216 binary PCM chunks, and max decoded peak about 0.5909 - Next step: restart Moshi and gateway normally, hard refresh browser, retest voice.
- 2026-06-12 15:38 IST - Codex - Phase 3 - Implemented ASK flow end-to-end in stub mode: answer jobs with correlation IDs, supersede/stale policy, answer and harmonizer prompts, TTS client boundary, TTS sidecar scaffold, outbound mixer injection, Moshi suppression during injection, expanded state machine, injection control messages, and Phase 3 acceptance tests - Files changed: gateway jobs/llm/session/speech/ws classes, prompts, `tts-service`, compose, README, memory files - Tests run: `mvn -pl gateway -Dtest=SessionStateMachineTests,Phase3AskFlowIntegrationTests test`, `mvn -pl gateway verify`, router-label validation, Python compile checks, JS syntax checks, `git diff --check` - Result: passed - Next step: stop for Phase 3 human checkpoint with real Moshi plus backend answer path.
- 2026-06-12 19:54 IST - Codex - Phase 3 TTS checkpoint fix - Replaced the local TTS sidecar's placeholder-only behavior with a macOS `say` + `afconvert` spoken WAV path while keeping deterministic tone fallback for non-macOS/CI; clarified that Java `TTS_MODE=stub` intentionally beeps - Files changed: `tts-service/app/main.py`, `tts-service/README.md`, README, memory files - Tests run: macOS `say`/`afconvert` WAV conversion probe, Python compile check - Result: sidecar can produce 24 kHz mono spoken WAV on this Mac - Next step: run `tts-service` and start the gateway with `TTS_MODE=real` to hear speech instead of beep.
- 2026-06-12 20:05 IST - Codex - TTS sidecar setup fix - Diagnosed failed `pydantic-core` install as Python 3.14/PyO3 compatibility with pinned dependencies; rebuilt `tts-service/.venv` using `/opt/homebrew/bin/python3.12`, verified `/health` returns `engine=macos_say`, verified `/speak` returns 24 kHz mono WAV, and updated setup docs plus `.gitignore` for nested virtualenvs - Files changed: `.gitignore`, README files, memory files - Tests run: TTS venv install, TTS `/health`, TTS `/speak`, WAV format check - Result: passed - Next step: start TTS sidecar from the Python 3.12 venv and run gateway with `TTS_MODE=real`.
